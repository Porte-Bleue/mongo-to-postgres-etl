# add dependencies. these will get pulled during the `dbt deps` process.
---
packages:
  - git: "https://{{env_var('DBT_ENV_SECRET_GIT_CREDENTIAL')}}@github.com/dbt-labs/dbt-utils.git" # git HTTPS URL
    revision: 0.8.6
